# Infrastructure

This directory will contain deployment and server-related assets when implementation begins.

Expected contents later:

- `docker-compose.yml`
- reverse proxy config
- deploy scripts
